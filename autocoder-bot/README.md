## Project Name

This is a Python web service project using Docker.

### Setup

1. Build the Docker image:
```
docker build -t my-python-service .
```
2. Run the Docker container:
```
docker run -d -p 8080:8080 my-python-service
```
