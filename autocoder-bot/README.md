# Python Web Service

This is a Python web service project using Flask and requests.

## Setup
1. Build the Docker image: `docker build -t my-web-service .`
2. Run the Docker container: `docker run -p 5000:5000 my-web-service`
