# Project Name
This is a simple Python web service containerized with Docker.

## How to build and run the container:
1. Build the Docker image: `docker build -t my-python-web-service .`
2. Run the container: `docker run -p 8000:8000 my-python-web-service`
3. Access the web service at http://localhost:8000/
