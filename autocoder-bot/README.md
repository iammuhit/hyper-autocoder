# Python Web Service

This is a Python web service containerized using Docker. To build and run the container, follow these steps:

1. Build the Docker image: `docker build -t python-web-service .`
2. Run the Docker container: `docker run -d -p 8000:8000 python-web-service`
