# Python Web Service

This is a production-ready Python web service using Flask and requests.

## Build and Run

1. Build the Docker container: `docker build -t my-web-service .`
2. Run the container: `docker run -d -p 8000:8000 my-web-service`
